# Copy to ml/.env (or reuse backend/.env — train_model loads both)

MONGO_URI=mongodb+srv://user:password@cluster.mongodb.net/smart_attendance

# Optional overrides
# MONGO_DB_NAME=smart_attendance
# ML_SHORTAGE_THRESHOLD_PCT=75
# ML_MIN_STUDENT_SAMPLES=10
# ML_MIN_SESSIONS_PER_STUDENT=1
# ML_MIN_NEW_RECORDS=20
# ML_TRAIN_SECRET=your_secret_for_train_endpoint
# ML_PORT=8000
